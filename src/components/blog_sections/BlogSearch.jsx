import React from 'react';
// import { BlogContext } from '../context/BlogContext';

function BlogSearch({children}) {

  return (
    <React.Fragment>
      {children}
    </React.Fragment>
  );
}

export { BlogSearch }